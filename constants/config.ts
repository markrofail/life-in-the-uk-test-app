export const AppConfig = {
    // Determine how many questions are drawn per practice exam
    EXAM_QUESTION_COUNT: 24,

    // The percentage required to pass
    // E.g., 75 is 75%
    PASSING_PERCENTAGE: 75,
};
